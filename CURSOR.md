# Using this repo with Cursor

Copy `.cursor/rules/calcinate.mdc` to your project's `.cursor/rules/` directory, or `CLAUDE.md` to the project root.

The rule is `alwaysApply: false` — invoke when you explicitly want calcination behavior, not as a constant background.
